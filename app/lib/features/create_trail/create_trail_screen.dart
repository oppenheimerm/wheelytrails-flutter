import 'package:flutter/material.dart';

class CreateTrailScreen extends StatelessWidget {
  const CreateTrailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Create Trail Screen'),
    );
  }
}
